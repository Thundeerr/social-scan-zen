# (No active plan)

Auto-publish to Instagram via Meta Graph API was scoped and partially built (schema + trigger landed in migrations), then shelved — Meta app review + OAuth setup is too heavy for now. Revisit via a lighter path (manual handoff tray, third-party scheduler bridge, or a different destination platform) when ready.
